import { ModelCheckpointCallback } from './model-checkpoint-callback';

describe('ModelCheckpointCallback', () => {
  it('should create an instance', () => {
    expect(new ModelCheckpointCallback()).toBeTruthy();
  });
});
