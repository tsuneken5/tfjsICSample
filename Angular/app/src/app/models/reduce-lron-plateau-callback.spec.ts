import { ReduceLROnPlateauCallback } from './reduce-lron-plateau-callback';

describe('ReduceLROnPlateauCallback', () => {
  it('should create an instance', () => {
    expect(new ReduceLROnPlateauCallback()).toBeTruthy();
  });
});
